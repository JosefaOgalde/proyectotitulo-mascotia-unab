"""
Configuración local de desarrollo.
Copia este archivo a local.py y ajusta los valores según tu entorno.
NUNCA subas local.py al repositorio (está en .gitignore).
"""

import os
import warnings

# Suprimir todos los warnings en la consola
warnings.filterwarnings('ignore')

from .base import *

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = os.environ.get('DJANGO_ALLOWED_HOSTS', '').split(',') if os.environ.get('DJANGO_ALLOWED_HOSTS') else []

# Base de datos SQLite
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}

# Static files (CSS, JavaScript, Images)
STATIC_URL = 'static/'

# Directorios donde Django buscará archivos estáticos
# La estructura es: mascotia/mascotia/registro/static/
STATICFILES_DIRS = [
    BASE_DIR / 'mascotia' / 'registro' / 'static',
]

# Directorio donde se recopilarán los archivos estáticos para producción
STATIC_ROOT = BASE_DIR / 'staticfiles'

# Media files (archivos subidos por usuarios)
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'

# SECRET_KEY para desarrollo local
# IMPORTANTE: Genera tu propia clave secreta para producción
# Puedes usar: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY = 'tu-secret-key-aqui-cambiar-en-produccion'

